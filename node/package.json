{
  "name": "proiot-node",
  "version": "1.0.0",
  "description": "Proiot node app",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node src/index",
    "dev": "ts-node-dev --poll --inspect --transpile-only --ignore-watch node_modules --respawn src/server.ts"
  },
  "author": "andrechism",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.8.4",
    "ws": "^8.11.0"
  },
  "devDependencies": {
    "@types/ws": "^8.5.3"
  }
}
