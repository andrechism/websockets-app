import styled from "styled-components"

export const ControlsContainer = styled.section`
    display: flex;
    width: 100%;

    padding: 1rem 1rem 0;

    & > * {
        margin-right: 1rem;
    }
`